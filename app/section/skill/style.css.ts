import { style } from "@vanilla-extract/css";

export const ProjectWrapperStyle = style({
  width: "100%",
  height: "auto",
  display: "flex",
  flexDirection: "column",
});

export const TitleStyle = style({
  fontSize: "24px",
  fontWeight: "bold",
});